.DS_Store
node_modules/
dist/
.astro/
# raw/ is tracked (needed at build time, incl. in CI for the GitHub Pages deploy) —
# see CLAUDE.md > Images. Never PSD/AI sources or archives, those stay local.
*.psd
*.psb
*.ai
*.zip
